import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'br.com.anhembi.nuzlockecreator',
  appName: 'Nuzlocke Creator',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
